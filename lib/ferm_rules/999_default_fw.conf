table filter {
 chain fw {
        daddr $ipv4_address proto tcp dport ssh ACCEPT;
        daddr $ipv4_address proto tcp dport 1194 ACCEPT;
    }
 chain INPUT {
		daddr $ipv4_address jump fw;
	mod state {
		state (RELATED ESTABLISHED) ACCEPT;
		}
}
}
